!function(){"use strict";var ws=new WebSocket("ws://"+location.hostname+":80");function eventConfig(e){e.target.closest("a");let d_button=e.target.closest("button");e.target.closest("div");if("click"==e.type&&d_button)if("submit"==d_button.id){e.preventDefault();document.getElementById("wifi_ssid").value,document.getElementById("wifi_pass").value;console.log("Saving settings...")}else if("reset_bag"==d_button.id){e.preventDefault();console.log("Resetting bag..."),ws.send(JSON.stringify({cmd:"reset_bag",volume_ml:500})),document.getElementById("output").value="Reset Bag",console.log("Reset...")}}ws.onmessage=function(evt){document.getElementById("output").value=evt.data},window.addEventListener("click",eventConfig),window.onload=function(){var wifiText;wifiText="Not connected to internet",document.getElementById("wifi_status").innerHTML=wifiText}}();